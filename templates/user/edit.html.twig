{% extends 'template.html.twig' %}

{% block titre %}Mon profil{% endblock %}

{% block body %}
    <h1>Mon profil</h1>
    <div class="float-center">
        <div class="grid-x grid-padding-x">
            <img src="{{ asset('images/' ~ user.getPhoto()) }}" alt="ma photo">
            <div class="float-center">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" href="{{ path('user_edit', {'id': user.id}) }}" class="success button">Enregistrer</button>
                <button class="alert button" type="reset">Annuler</button>


                {{ form_end(form) }}

            </div>
        </div>
    </div>



{#  {{ include('user/_delete_form.html.twig') }}#}
{% endblock %}
