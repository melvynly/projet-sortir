{% extends 'template.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
{% for user in users %}
    <table class="table">
        <tbody>
        <tr>
            <td><img src="{{ asset('images/' ~ user.getPhoto()) }}" alt="ma photo"></td>
            <th>
                <h1>{{ user.pseudo }}</h1>
            </th>

        </tr>
        <tr>
            <th>Prenom : </th>
            <td>{{ user.prenom }}</td>
        </tr>
        <tr>
            <th>Nom : </th>
            <td>{{ user.nom }}</td>
        </tr>

        <tr>
            <th>Telephone : </th>
            <td>{{ user.telephone }}</td>
        </tr>
        <tr>
            <th>Email : </th>
            <td>{{ user.mail }}</td>
        </tr>
        <tr>
            <th>Ville de rattachement : </th>
            <td>{{ user.site.nom }}</td>
        </tr>

        </tbody>
    </table>

                    <a href="{{ path('user_show', {'id': user.id}) }}">show</a>
                    <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>

        {% else %}

        {% endfor %}>

    <a href="{{ path('user_new') }}" class="button">Créer un user</a>
    <a href="#" class="button">Retour</a>
{% endblock %}
